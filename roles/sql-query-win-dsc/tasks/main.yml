---

- name: Setup the SqlServerDsc module
  win_psmodule:
    name: SqlServerDsc
    state: present

- name: Run query
  win_dsc:
    resource_name: SqlScriptQuery
    ServerInstance: '{{ ansible_host }}\{{ instance_name }}'
    GetQuery: "select * from rhdemo1.dbo"
  register: sql_query

- name: debug sql_query
  debug:
    var: sql_query